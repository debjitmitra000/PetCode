# Exclude source TypeScript — only compiled JS in out/ is needed
src/**
tsconfig.json

# Exclude dev dependencies
node_modules/**
package-lock.json

# Exclude test files
.vscode-test/**

# 🚨 Exclude heavy assets (THIS WAS MISSING)
images/**
icons/**

# Exclude git stuff
.git
.gitignore

# Optional: exclude docs
*.md
deploy.md
filetree.md
changelog.md